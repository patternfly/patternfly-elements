{
  "text": {
    "default": { "value": "$pf-color-black-800" },
    "on-dark": { "value": "$pf-color-white" },
    "on-saturated": { "value": "$pf-color-white" }
  },
  "link": {
    "default": { "value": "$pf-color-blue-400" },
    "on-dark": { "value": "$pf-color-blue-100" },
    "on-saturated": { "value": "$pf-color-white" },
    "visited": {
      "default": { "value": "$pf-color-purple-600" },
      "on-dark": { "value": "$pf-color-purple-300" },
      "on-saturated": { "value": "$pf-color-purple-300" }
    },
    "hover": {
      "default": { "value": "$pf-color-blue-500" },
      "on-dark": { "value": "$pf-color-blue-50" },
      "on-saturated": { "value": "$pf-color-white" }
    },
    "focus": {
      "default": { "value": "$pf-color-blue-500" },
      "on-dark": { "value": "$pf-color-blue-50" },
      "on-saturated": { "value": "$pf-color-white" }
    }
  },
  "link-decoration": {
    "default": { "value": "none" },
    "on-dark": { "value": "none" },
    "on-saturated": { "value": "underline" },
    "visited": {
      "default": { "value": "none" },
      "on-dark": { "value": "none" },
      "on-saturated": { "value": "underline" }
    },
    "hover": {
      "default": { "value": "underline" },
      "on-dark": { "value": "underline" },
      "on-saturated": { "value": "underline" }
    },
    "focus": {
      "default": { "value": "underline" },
      "on-dark": { "value": "underline" },
      "on-saturated": { "value": "underline" }
    }
  }
}
